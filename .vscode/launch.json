{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug superstar hello",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "args": ["hello"]
    },
    {
      "name": "Debug superstar (prompt for args)",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "args": "${input:cliArgs}"
    }
  ],
  "inputs": [
    {
      "id": "cliArgs",
      "type": "promptString",
      "description": "CLI args (space-separated)",
      "default": "hello"
    }
  ]
}
